<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    Shell.NavBarIsVisible="False"
    Title="Конфликты лекарств"
    x:Class="MedCompatibility.Pages.Admin.InteractionsListPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:converters="clr-namespace:MedCompatibility.Converters"
    xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
    xmlns:models="clr-namespace:MedCompatibility.Models"
    xmlns:vm="clr-namespace:MedCompatibility.ViewModels.Admin"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:IsNullConverter x:Key="IsNullConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>

        <!--  фон  -->
        <controls:BackgroundBubblesView
            BottomBubbleBounds="0.95, 0.45, 520, 520"
            RightBubbleBounds="0.08, 0.98, 760, 760"
            TopBubbleBounds="0.52, 0.08, 280, 280" />

        <Grid RowDefinitions="Auto, *, Auto" Style="{StaticResource PageContentGrid}">

            <!--  1) Шапка  -->
            <controls:CustomHeader Margin="10,10,10,5" Title="Взаимодействия" />

            <!--  2) Список и фильтры  -->
            <Grid Grid.Row="1" Padding="10,0" RowDefinitions="Auto, *">

                <!--  Фильтры  -->
                <Frame
                    Grid.Row="0"
                    Margin="0,5"
                    Padding="16"
                    Style="{StaticResource MedCard}">
                    <VerticalStackLayout Spacing="12">

                        <VerticalStackLayout Spacing="4">
                            <Label
                                FontAttributes="Bold"
                                FontSize="12"
                                Text="Поиск по веществу"
                                TextColor="{StaticResource TextSecondary}" />
                            <Border HeightRequest="44" Style="{StaticResource MedInputContainer}">
                                <Entry
                                    FontSize="14"
                                    Placeholder="Введите название..."
                                    ReturnCommand="{Binding LoadDataCommand}"
                                    Text="{Binding SearchText}" />
                            </Border>
                        </VerticalStackLayout>

                        <Grid ColumnDefinitions="*, *" ColumnSpacing="12">
                            <VerticalStackLayout Spacing="4">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="12"
                                    Text="Уровень риска"
                                    TextColor="{StaticResource TextSecondary}" />
                                <Border HeightRequest="44" Style="{StaticResource MedInputContainer}">
                                    <Picker
                                        FontSize="13"
                                        ItemDisplayBinding="{Binding Name}"
                                        ItemsSource="{Binding RiskLevels}"
                                        SelectedItem="{Binding SelectedRiskLevel}"
                                        TextColor="{StaticResource TextPrimary}" />
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="4">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="12"
                                    Text="Тип"
                                    TextColor="{StaticResource TextSecondary}" />
                                <Border HeightRequest="44" Style="{StaticResource MedInputContainer}">
                                    <Picker
                                        FontSize="13"
                                        ItemDisplayBinding="{Binding Name}"
                                        ItemsSource="{Binding InteractionTypes}"
                                        SelectedItem="{Binding SelectedInteractionType}"
                                        TextColor="{StaticResource TextPrimary}" />
                                </Border>
                            </VerticalStackLayout>
                        </Grid>

                        <Grid
                            ColumnDefinitions="Auto,*"
                            ColumnSpacing="10"
                            Margin="0,5,0,0">
                            <Button
                                Command="{Binding ResetFiltersCommand}"
                                FontAttributes="Bold"
                                FontSize="13"
                                HeightRequest="44"
                                Style="{StaticResource TextButton}"
                                Text="Сброс"
                                TextColor="{StaticResource Error}" />
                            <Button
                                Command="{Binding LoadDataCommand}"
                                CornerRadius="10"
                                FontAttributes="Bold"
                                FontSize="14"
                                Grid.Column="1"
                                HeightRequest="44"
                                Style="{StaticResource PrimaryButton}"
                                Text="Найти" />
                        </Grid>
                    </VerticalStackLayout>
                </Frame>

                <RefreshView
                    Command="{Binding LoadDataCommand}"
                    Grid.Row="1"
                    IsRefreshing="{Binding IsBusy}"
                    Margin="0,5,0,0">

                    <CollectionView ItemsSource="{Binding Interactions}" SelectionMode="None" VerticalScrollBarVisibility="Never">

                        <CollectionView.EmptyView>
                            <Grid HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                                <Label
                                    HorizontalOptions="Center"
                                    Text="Взаимодействия не найдены."
                                    TextColor="{StaticResource TextSecondary}"
                                    VerticalOptions="Center" />
                            </Grid>
                        </CollectionView.EmptyView>

                        <CollectionView.ItemsLayout>
                            <LinearItemsLayout ItemSpacing="15" Orientation="Vertical" />
                        </CollectionView.ItemsLayout>

                        <CollectionView.Header>
                            <BoxView Color="Transparent" HeightRequest="10" />
                        </CollectionView.Header>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:interaction">
                                <Frame
                                    Margin="0,0,0,12"
                                    Padding="15"
                                    Style="{StaticResource MedListCard}">

                                    <!--  Редактирование по тапу на карточку  -->
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:InteractionsListViewModel}}, Path=EditInteractionCommand}" CommandParameter="{Binding .}" />
                                    </Frame.GestureRecognizers>

                                    <Grid RowDefinitions="Auto,Auto,Auto" RowSpacing="8">

                                        <!--  Заголовок — явные цвета на каждом Span чтобы избежать бага наследования при переносе строки  -->
                                        <Label
                                            FontAttributes="Bold"
                                            FontSize="16"
                                            LineBreakMode="WordWrap"
                                            TextColor="{StaticResource TextPrimary}">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{Binding SubstanceId1Navigation.Name}" TextColor="{StaticResource TextPrimary}" />
                                                    <Span Text=" + " TextColor="#CCC" />
                                                    <Span Text="{Binding SubstanceId2Navigation.Name}" TextColor="{StaticResource TextPrimary}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>

                                        <!--  Риск + тип + удалить  -->
                                        <Grid
                                            ColumnDefinitions="Auto,*,Auto"
                                            ColumnSpacing="10"
                                            Grid.Row="1">

                                            <!--  Бэйдж риска  -->
                                            <Border
                                                Padding="10,5"
                                                StrokeShape="RoundRectangle 8"
                                                StrokeThickness="0"
                                                VerticalOptions="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="BackgroundColor" Value="#E0F7FA" />
                                                        <Style.Triggers>
                                                            <DataTrigger
                                                                Binding="{Binding RiskLevelId}"
                                                                TargetType="Border"
                                                                Value="3">
                                                                <Setter Property="BackgroundColor" Value="#FFEBEE" />
                                                            </DataTrigger>
                                                            <DataTrigger
                                                                Binding="{Binding RiskLevelId}"
                                                                TargetType="Border"
                                                                Value="2">
                                                                <Setter Property="BackgroundColor" Value="#FFF3E0" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>

                                                <Label
                                                    FontAttributes="Bold"
                                                    FontSize="11"
                                                    Text="{Binding RiskLevel.Name}">
                                                    <Label.Style>
                                                        <Style TargetType="Label">
                                                            <Setter Property="TextColor" Value="#006064" />
                                                            <Style.Triggers>
                                                                <DataTrigger
                                                                    Binding="{Binding RiskLevelId}"
                                                                    TargetType="Label"
                                                                    Value="3">
                                                                    <Setter Property="TextColor" Value="#C62828" />
                                                                </DataTrigger>
                                                                <DataTrigger
                                                                    Binding="{Binding RiskLevelId}"
                                                                    TargetType="Label"
                                                                    Value="2">
                                                                    <Setter Property="TextColor" Value="#EF6C00" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Label.Style>
                                                </Label>
                                            </Border>

                                            <Label
                                                FontSize="12"
                                                Grid.Column="1"
                                                LineBreakMode="TailTruncation"
                                                Text="{Binding InteractionType.Name}"
                                                TextColor="{StaticResource TextSecondary}"
                                                VerticalOptions="Center" />

                                            <Button
                                                BackgroundColor="Transparent"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:InteractionsListViewModel}}, Path=DeleteInteractionCommand}"
                                                CommandParameter="{Binding .}"
                                                FontSize="18"
                                                Grid.Column="2"
                                                HeightRequest="30"
                                                Padding="0"
                                                Text="🗑"
                                                TextColor="{StaticResource Error}"
                                                WidthRequest="30" />
                                        </Grid>

                                        <!--  Описание  -->
                                        <Label
                                            FontSize="13"
                                            Grid.Row="2"
                                            Text="{Binding Description}"
                                            TextColor="{StaticResource TextPrimary}" />
                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>
                </RefreshView>
            </Grid>

            <Grid Grid.Row="2" Padding="0,10,0,10">
                <Button
                    Command="{Binding GoToAddInteractionCommand}"
                    CornerRadius="28"
                    FontSize="30"
                    HeightRequest="56"
                    HorizontalOptions="Center"
                    Padding="0"
                    Style="{StaticResource PrimaryButton}"
                    Text="+"
                    TextColor="White"
                    VerticalOptions="Center"
                    WidthRequest="56">
                    <Button.Shadow>
                        <Shadow
                            Brush="{StaticResource Primary}"
                            Offset="0,4"
                            Opacity="0.3"
                            Radius="8" />
                    </Button.Shadow>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</ContentPage>
