<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    Shell.NavBarIsVisible="False"
    x:Class="MedCompatibility.Pages.Shared.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid>
        <controls:BackgroundBubblesView 
            TopBubbleBounds="0.52, 0.08, 260, 260" 
            RightBubbleBounds="0.05, 0.80, 680, 680" 
            BottomBubbleBounds="0.95, 0.92, 460, 460" />
        <!-- <AbsoluteLayout InputTransparent="True"> -->
        <!--  ~1~  Большой слева снизу  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.05,0.80,680,680"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#140097A7"  -->
        <!--  CornerRadius="340"  -->
        <!--  HeightRequest="680"  -->
        <!--  WidthRequest="680" />  -->
        <!--    -->
        <!--  ~1~  Средний справа снизу  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.95,0.92,460,460"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#0F90A4AE"  -->
        <!--  CornerRadius="230"  -->
        <!--  HeightRequest="460"  -->
        <!--  WidthRequest="460" />  -->
        <!--    -->
        <!--  ~1~  Маленький сверху  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.52,0.08,260,260"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#1A00796B"  -->
        <!--  CornerRadius="130"  -->
        <!--  HeightRequest="260"  -->
        <!--  WidthRequest="260" />  -->
        <!-- </AbsoluteLayout> -->

        <ScrollView VerticalScrollBarVisibility="Never">
            <VerticalStackLayout
                HorizontalOptions="Center"
                MaximumWidthRequest="440"
                Padding="20"
                Spacing="16"
                VerticalOptions="Center">

                <!--  Шапка: стикер + название + слоган  -->
                <VerticalStackLayout HorizontalOptions="Center" Spacing="8">

                    <Border
                        BackgroundColor="{StaticResource Surface}"
                        HeightRequest="54"
                        HorizontalOptions="Center"
                        Padding="10"
                        Stroke="#E6EAF0"
                        StrokeShape="RoundRectangle 999"
                        StrokeThickness="1"
                        WidthRequest="54">
                        <Border.Shadow>
                            <Shadow
                                Brush="#90A4AE"
                                Offset="0,8"
                                Opacity="0.10"
                                Radius="20" />
                        </Border.Shadow>

                        <Label
                            FontSize="26"
                            HorizontalOptions="Center"
                            Text="🩺"
                            VerticalOptions="Center" />
                    </Border>

                    <Label
                        FontAttributes="Bold"
                        FontSize="26"
                        HorizontalTextAlignment="Center"
                        Text="MedCompatibility"
                        TextColor="{StaticResource TextPrimary}" />

                    <Label
                        FontSize="13"
                        HorizontalTextAlignment="Center"
                        Text="Безопасность вашего здоровья"
                        TextColor="{StaticResource TextSecondary}" />

                </VerticalStackLayout>

                <!--  Карточка логина  -->
                <Frame
                    Padding="22"
                    Style="{StaticResource CardFrame}"
                    WidthRequest="380">

                    <VerticalStackLayout Spacing="14">

                        <VerticalStackLayout Spacing="6">
                            <Label
                                FontSize="12"
                                Text="Логин"
                                TextColor="{StaticResource TextSecondary}" />
                            <Border Style="{StaticResource InputFrame}">
                                <Entry
                                    Placeholder="Введите ваш логин"
                                    PlaceholderColor="{StaticResource TextPlaceholder}"
                                    Text="{Binding Login}"
                                    TextColor="{StaticResource TextPrimary}" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label
                                FontSize="12"
                                Text="Пароль"
                                TextColor="{StaticResource TextSecondary}" />
                            <Border Style="{StaticResource InputFrame}">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Entry
                                        Grid.Column="0"
                                        IsPassword="{Binding IsPasswordHidden}"
                                        Placeholder="Введите ваш пароль"
                                        PlaceholderColor="{StaticResource TextPlaceholder}"
                                        Text="{Binding Password}"
                                        TextColor="{StaticResource TextPrimary}" />
                                    <Button
                                        BackgroundColor="Transparent"
                                        Command="{Binding TogglePasswordVisibilityCommand}"
                                        Grid.Column="1"
                                        HeightRequest="46"
                                        Padding="0"
                                        Text="{Binding EyeIconText}"
                                        TextColor="{StaticResource TextSecondary}"
                                        WidthRequest="46" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>

                        <Label
                            FontSize="13"
                            HorizontalTextAlignment="Center"
                            IsVisible="{Binding IsErrorVisible}"
                            Text="{Binding ErrorMessage}"
                            TextColor="{StaticResource Error}" />

                        <Button
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Text="Войти" />

                        <Button
                            Command="{Binding GoogleLoginCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Text="Продолжить с Google" />

                        <!--  Разделитель  -->
                        <Grid
                            ColumnDefinitions="*,Auto,*"
                            ColumnSpacing="12"
                            Margin="0,2,0,0">
                            <BoxView
                                Color="#E6EAF0"
                                HeightRequest="1"
                                VerticalOptions="Center" />
                            <Label
                                FontSize="12"
                                Grid.Column="1"
                                Text="Или"
                                TextColor="{StaticResource TextSecondary}"
                                VerticalOptions="Center" />
                            <BoxView
                                Color="#E6EAF0"
                                Grid.Column="2"
                                HeightRequest="1"
                                VerticalOptions="Center" />
                        </Grid>

                        <!--  Регистрация  -->
                        <Button
                            Command="{Binding GoToRegisterCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Text="Создать аккаунт" />

                        <!--  Гость  -->
                        <Button
                            Command="{Binding GuestLoginCommand}"
                            HorizontalOptions="Center"
                            Style="{StaticResource TextButton}"
                            Text="Продолжить без регистрации"
                            TextColor="{StaticResource TextSecondary}" />

                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

    </Grid>
</ContentPage>
