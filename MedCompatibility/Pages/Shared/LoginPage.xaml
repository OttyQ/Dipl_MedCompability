<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    Shell.NavBarIsVisible="False"
    x:Class="MedCompatibility.Pages.Shared.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid>
        <controls:BackgroundBubblesView 
            TopBubbleBounds="0.52, 0.08, 260, 260" 
            RightBubbleBounds="0.05, 0.80, 680, 680" 
            BottomBubbleBounds="0.95, 0.92, 460, 460" />
        <!-- <AbsoluteLayout InputTransparent="True"> -->
        <!--  ~1~  Большой слева снизу  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.05,0.80,680,680"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#140097A7"  -->
        <!--  CornerRadius="340"  -->
        <!--  HeightRequest="680"  -->
        <!--  WidthRequest="680" />  -->
        <!--    -->
        <!--  ~1~  Средний справа снизу  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.95,0.92,460,460"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#0F90A4AE"  -->
        <!--  CornerRadius="230"  -->
        <!--  HeightRequest="460"  -->
        <!--  WidthRequest="460" />  -->
        <!--    -->
        <!--  ~1~  Маленький сверху  @1@  -->
        <!--  <BoxView  -->
        <!--  AbsoluteLayout.LayoutBounds="0.52,0.08,260,260"  -->
        <!--  AbsoluteLayout.LayoutFlags="PositionProportional"  -->
        <!--  Color="#1A00796B"  -->
        <!--  CornerRadius="130"  -->
        <!--  HeightRequest="260"  -->
        <!--  WidthRequest="260" />  -->
        <!-- </AbsoluteLayout> -->

        <ScrollView VerticalScrollBarVisibility="Never">
            <VerticalStackLayout
                HorizontalOptions="Center"
                MaximumWidthRequest="{OnIdiom Phone=440, Desktop=500}"
                Padding="{OnIdiom Phone='20', Desktop='32'}"
                Spacing="{OnIdiom Phone=16, Desktop=24}"
                VerticalOptions="Center">

                <!--  Шапка: стикер + название + слоган  -->
                <VerticalStackLayout HorizontalOptions="Center" Spacing="8">

                    <Image 
                        Source="logo.png" 
                        HeightRequest="120" 
                        WidthRequest="120" 
                        HorizontalOptions="Center" 
                        Margin="0, 20, 0, 30" />

                    <Label
                        FontAttributes="Bold"
                        FontSize="{OnIdiom Phone=32, Desktop=36}"
                        HorizontalTextAlignment="Center">
                        <Label.FormattedText>
                            <FormattedString>
                                <Span Text="Veri" TextColor="{StaticResource TextPrimary}" />
                                <Span Text="Pharm" TextColor="{StaticResource Primary}" />
                            </FormattedString>
                        </Label.FormattedText>
                    </Label>

                    <Label
                        FontSize="{OnIdiom Phone=13, Desktop=15}"
                        HorizontalTextAlignment="Center"
                        Text="Безопасность вашего здоровья"
                        TextColor="{StaticResource TextSecondary}" />

                </VerticalStackLayout>

                <!--  Карточка логина  -->
                <Frame
                    Padding="{OnIdiom Phone=22, Desktop=30}"
                    Style="{StaticResource CardFrame}"
                    WidthRequest="{OnIdiom Phone=380, Desktop=440}">

                    <VerticalStackLayout Spacing="{OnIdiom Phone=14, Desktop=18}">

                        <VerticalStackLayout Spacing="6">
                            <Label
                                FontSize="{OnIdiom Phone=12, Desktop=14}"
                                Text="Логин"
                                TextColor="{StaticResource TextSecondary}" />
                            <Border Style="{StaticResource InputFrame}">
                                <Entry
                                    Placeholder="Введите ваш логин"
                                    PlaceholderColor="{StaticResource TextPlaceholder}"
                                    Text="{Binding Login}"
                                    TextColor="{StaticResource TextPrimary}" />
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout Spacing="6">
                            <Label
                                FontSize="{OnIdiom Phone=12, Desktop=14}"
                                Text="Пароль"
                                TextColor="{StaticResource TextSecondary}" />
                            <Border Style="{StaticResource InputFrame}">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Entry
                                        Grid.Column="0"
                                        IsPassword="{Binding IsPasswordHidden}"
                                        Placeholder="Введите ваш пароль"
                                        PlaceholderColor="{StaticResource TextPlaceholder}"
                                        Text="{Binding Password}"
                                        TextColor="{StaticResource TextPrimary}" />
                                    <Button
                                        BackgroundColor="Transparent"
                                        Command="{Binding TogglePasswordVisibilityCommand}"
                                        Grid.Column="1"
                                        HeightRequest="46"
                                        Padding="0"
                                        Text="{Binding EyeIconText}"
                                        TextColor="{StaticResource TextSecondary}"
                                        WidthRequest="46" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>

                        <!-- Согласие на обработку ПД -->
                        <Grid ColumnDefinitions="Auto, *" ColumnSpacing="{OnIdiom Phone=6, Desktop=2}" Margin="0,4,0,0">
                            <CheckBox
                                Grid.Column="0"
                                Color="{StaticResource Primary}"
                                IsChecked="{Binding IsDataProcessingAccepted}"
                                WidthRequest="{OnIdiom Phone=-1, Desktop=40}"
                                MinimumWidthRequest="{OnIdiom Phone=-1, Desktop=40}"
                                VerticalOptions="Start" />
                            <Label
                                Grid.Column="1"
                                FontSize="{OnIdiom Phone=12, Desktop=13}"
                                LineBreakMode="WordWrap"
                                MaximumWidthRequest="320"
                                VerticalOptions="Center">
                                <Label.FormattedText>
                                    <FormattedString>
                                        <Span Text="Я даю согласие на " TextColor="{StaticResource TextSecondary}" />
                                        <Span Text="обработку моих персональных данных" TextColor="{StaticResource Primary}" TextDecorations="Underline">
                                            <Span.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding ShowPrivacyPolicyCommand}" />
                                            </Span.GestureRecognizers>
                                        </Span>
                                        <Span Text=" и принимаю условия " TextColor="{StaticResource TextSecondary}" />
                                        <Span Text="Политики конфиденциальности" TextColor="{StaticResource Primary}" TextDecorations="Underline">
                                            <Span.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding ShowPrivacyPolicyCommand}" />
                                            </Span.GestureRecognizers>
                                        </Span>
                                    </FormattedString>
                                </Label.FormattedText>
                            </Label>
                        </Grid>

                        <Label
                            FontSize="{OnIdiom Phone=13, Desktop=14}"
                            HorizontalTextAlignment="Center"
                            IsVisible="{Binding IsErrorVisible}"
                            Text="{Binding ErrorMessage}"
                            TextColor="{StaticResource Error}" />

                        <Button
                            Command="{Binding LoginCommand}"
                            Style="{StaticResource PrimaryButton}"
                            Text="Войти" />

                        <Button
                            Command="{Binding GoogleLoginCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Text="Продолжить с Google" />

                        <!--  Разделитель  -->
                        <Grid
                            ColumnDefinitions="*,Auto,*"
                            ColumnSpacing="12"
                            Margin="0,2,0,0">
                            <BoxView
                                Color="#E6EAF0"
                                HeightRequest="1"
                                VerticalOptions="Center" />
                            <Label
                                FontSize="12"
                                Grid.Column="1"
                                Text="Или"
                                TextColor="{StaticResource TextSecondary}"
                                VerticalOptions="Center" />
                            <BoxView
                                Color="#E6EAF0"
                                Grid.Column="2"
                                HeightRequest="1"
                                VerticalOptions="Center" />
                        </Grid>

                        <!--  Регистрация  -->
                        <Button
                            Command="{Binding GoToRegisterCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Text="Создать аккаунт" />

                        <!--  Гость  -->
                        <Button
                            Command="{Binding GuestLoginCommand}"
                            HorizontalOptions="Center"
                            Style="{StaticResource TextButton}"
                            Text="Продолжить без регистрации"
                            TextColor="{StaticResource TextSecondary}" />

                    </VerticalStackLayout>
                </Frame>

            </VerticalStackLayout>
        </ScrollView>

        <Button
            BackgroundColor="{StaticResource Surface}"
            BorderColor="{StaticResource Primary}"
            BorderWidth="1"
            Command="{Binding OpenDebugPopupCommand}"
            CornerRadius="22"
            HeightRequest="44"
            HorizontalOptions="End"
            IsVisible="{Binding IsDebugMode}"
            Margin="20"
            Padding="0"
            Text="🛠️"
            VerticalOptions="End"
            WidthRequest="44">
            <Button.Shadow>
                <Shadow
                    Brush="Black"
                    Offset="0,2"
                    Opacity="0.2"
                    Radius="5" />
            </Button.Shadow>
        </Button>

    </Grid>
</ContentPage>

