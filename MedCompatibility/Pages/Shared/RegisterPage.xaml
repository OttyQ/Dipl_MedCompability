<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    BackgroundColor="{StaticResource AppBackground}"
    Shell.NavBarIsVisible="False"
    x:Class="MedCompatibility.Pages.Shared.RegisterPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid>
        <!--  фон  -->
        <controls:BackgroundBubblesView BottomBubbleBounds="0, 0, 0, 0" />

        <ScrollView VerticalScrollBarVisibility="Never">
            <Grid Padding="{OnIdiom Phone='20', Desktop='32'}" RowDefinitions="*">

                <VerticalStackLayout
                    HorizontalOptions="Center"
                    MaximumWidthRequest="{OnIdiom Phone=440, Desktop=520}"
                    Spacing="{OnIdiom Phone=25, Desktop=30}"
                    VerticalOptions="Center">

                    <!--  Заголовок  -->
                    <VerticalStackLayout HorizontalOptions="Center" Spacing="5">
                        <Label
                            HorizontalTextAlignment="Center"
                            Style="{StaticResource HeaderLabel}"
                            Text="Новый аккаунт" />
                        <Label
                            HorizontalTextAlignment="Center"
                            Style="{StaticResource SubHeaderLabel}"
                            Text="Заполните данные для входа" />
                    </VerticalStackLayout>

                    <!--  Карточка регистрации  -->
                    <Frame
                        BackgroundColor="{StaticResource Surface}"
                        CornerRadius="24"
                        HasShadow="True"
                        Padding="{OnIdiom Phone=24, Desktop=32}"
                        WidthRequest="{OnIdiom Phone=380, Desktop=460}">
                        <VerticalStackLayout Spacing="{OnIdiom Phone=15, Desktop=18}">

                            <!--  Блок ФИО  -->
                            <VerticalStackLayout Spacing="10">

                                <!--  Фамилия (на всю ширину)  -->
                                <VerticalStackLayout Spacing="6">
                                    <Label
                                        FontAttributes="Bold"
                                        FontSize="{OnIdiom Phone=13, Desktop=14}"
                                        Text="Фамилия"
                                        TextColor="{StaticResource TextPrimary}" />
                                    <Border Style="{StaticResource InputFrame}">
                                        <Entry Placeholder="Иванов" Text="{Binding LastName}" />
                                    </Border>
                                </VerticalStackLayout>

                                <!--  Имя и Отчество (в один ряд)  -->
                                <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                                    <VerticalStackLayout Grid.Column="0" Spacing="6">
                                        <Label
                                            FontAttributes="Bold"
                                            FontSize="{OnIdiom Phone=13, Desktop=14}"
                                            Text="Имя"
                                            TextColor="{StaticResource TextPrimary}" />
                                        <Border Style="{StaticResource InputFrame}">
                                            <Entry Placeholder="Иван" Text="{Binding FirstName}" />
                                        </Border>
                                    </VerticalStackLayout>

                                    <VerticalStackLayout Grid.Column="1" Spacing="6">
                                        <Label
                                            FontAttributes="Bold"
                                            FontSize="{OnIdiom Phone=13, Desktop=14}"
                                            Text="Отчество"
                                            TextColor="{StaticResource TextPrimary}" />
                                        <Border Style="{StaticResource InputFrame}">
                                            <Entry Placeholder="Иванович" Text="{Binding MiddleName}" />
                                        </Border>
                                    </VerticalStackLayout>
                                </Grid>

                            </VerticalStackLayout>

                            <!--  Логин  -->
                            <VerticalStackLayout Spacing="6">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="{OnIdiom Phone=13, Desktop=14}"
                                    Text="Логин"
                                    TextColor="{StaticResource TextPrimary}" />
                                <Border Style="{StaticResource InputFrame}">
                                    <Entry Placeholder="Придумайте логин" Text="{Binding Login}" />
                                </Border>
                            </VerticalStackLayout>

                            <!--  Пароль  -->
                            <VerticalStackLayout Spacing="6">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="{OnIdiom Phone=13, Desktop=14}"
                                    Text="Пароль"
                                    TextColor="{StaticResource TextPrimary}" />
                                <Border Style="{StaticResource InputFrame}">
                                    <Entry
                                        IsPassword="True"
                                        Placeholder="Минимум 6 символов"
                                        Text="{Binding Password}" />
                                </Border>
                            </VerticalStackLayout>

                            <!--  Подтверждение пароля  -->
                            <VerticalStackLayout Spacing="6">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="{OnIdiom Phone=13, Desktop=14}"
                                    Text="Повторите пароль"
                                    TextColor="{StaticResource TextPrimary}" />
                                <Border Style="{StaticResource InputFrame}">
                                    <Entry
                                        IsPassword="True"
                                        Placeholder="Пароль ещё раз"
                                        Text="{Binding ConfirmPassword}" />
                                </Border>
                            </VerticalStackLayout>

                            <!--  Выбор роли  -->
                            <VerticalStackLayout Spacing="6">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="{OnIdiom Phone=13, Desktop=14}"
                                    Text="Тип аккаунта"
                                    TextColor="{StaticResource TextPrimary}" />

                                <Border Style="{StaticResource InputFrame}">
                                    <Picker
                                        BackgroundColor="Transparent"
                                        HorizontalOptions="Fill"
                                        ItemsSource="{Binding AvailableRoles}"
                                        SelectedItem="{Binding SelectedRole}"
                                        TextColor="{StaticResource TextPrimary}"
                                        TitleColor="{StaticResource TextSecondary}" />
                                </Border>

                                <!--  Инфо-сообщение (появляется при выборе Врача)  -->
                                <Border
                                    BackgroundColor="#E3F2FD"
                                    Margin="0,5,0,0"
                                    Padding="10"
                                    StrokeShape="RoundRectangle 8"
                                    StrokeThickness="0">
                                    <Border.Triggers>
                                        <DataTrigger
                                            Binding="{Binding SelectedRole}"
                                            TargetType="Border"
                                            Value="Врач">
                                            <Setter Property="IsVisible" Value="True" />
                                        </DataTrigger>
                                        <DataTrigger
                                            Binding="{Binding SelectedRole}"
                                            TargetType="Border"
                                            Value="Пациент">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                        <DataTrigger
                                            Binding="{Binding SelectedRole}"
                                            TargetType="Border"
                                            Value="{x:Null}">
                                            <Setter Property="IsVisible" Value="False" />
                                        </DataTrigger>
                                    </Border.Triggers>

                                    <Label
                                        FontSize="11"
                                        Text="⚠️ Аккаунт врача требует подтверждения администратора"
                                        TextColor="{StaticResource Primary}" />
                                </Border>
                            </VerticalStackLayout>

                            <!-- Согласие на обработку ПД -->
                            <Grid ColumnDefinitions="Auto, *" ColumnSpacing="{OnIdiom Phone=6, Desktop=2}" Margin="0,4,0,0">
                                <CheckBox
                                    Grid.Column="0"
                                    Color="{StaticResource Primary}"
                                    IsChecked="{Binding IsDataProcessingAccepted}"
                                    WidthRequest="{OnIdiom Phone=-1, Desktop=40}"
                                    MinimumWidthRequest="{OnIdiom Phone=-1, Desktop=40}"
                                    VerticalOptions="Start" />
                                <Label
                                    Grid.Column="1"
                                    FontSize="{OnIdiom Phone=12, Desktop=13}"
                                    LineBreakMode="WordWrap"
                                    MaximumWidthRequest="340"
                                    VerticalOptions="Center">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Я даю согласие на " TextColor="{StaticResource TextSecondary}" />
                                            <Span Text="обработку моих персональных данных" TextColor="{StaticResource Primary}" TextDecorations="Underline">
                                                <Span.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding ShowPrivacyPolicyCommand}" />
                                                </Span.GestureRecognizers>
                                            </Span>
                                            <Span Text=" и принимаю условия " TextColor="{StaticResource TextSecondary}" />
                                            <Span Text="Политики конфиденциальности" TextColor="{StaticResource Primary}" TextDecorations="Underline">
                                                <Span.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding ShowPrivacyPolicyCommand}" />
                                                </Span.GestureRecognizers>
                                            </Span>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </Grid>

                            <!--  Ошибка  -->
                            <Label
                                FontSize="{OnIdiom Phone=13, Desktop=14}"
                                HorizontalTextAlignment="Center"
                                IsVisible="{Binding IsErrorVisible}"
                                Text="{Binding ErrorMessage}"
                                TextColor="{StaticResource Error}" />

                            <!--  Кнопка Регистрации  -->
                            <Button
                                Command="{Binding RegisterCommand}"
                                Margin="0,10,0,0"
                                Style="{StaticResource PrimaryButton}"
                                Text="Зарегистрироваться" />

                        </VerticalStackLayout>
                    </Frame>

                    <!--  Кнопка Назад  -->
                    <Button
                        Command="{Binding GoBackCommand}"
                        HorizontalOptions="Center"
                        Style="{StaticResource TextButton}"
                        Text="У меня уже есть аккаунт" />

                </VerticalStackLayout>
            </Grid>
        </ScrollView>
    </Grid>
</ContentPage>
