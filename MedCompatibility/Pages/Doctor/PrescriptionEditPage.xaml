<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    Shell.NavBarIsVisible="False"
    x:Class="MedCompatibility.Pages.Doctor.PrescriptionEditPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:vm="clr-namespace:MedCompatibility.ViewModels.Doctor"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid RowDefinitions="Auto,*" Style="{StaticResource PageContentGrid}" WidthRequest="{OnPlatform WinUI=800, MacCatalyst=800}">

        <!--  HEADER  -->
        <controls:CustomHeader
            Grid.Row="0"
            Margin="20,20,20,10"
            Title="{Binding PageTitle}" />

        <!--  CONTENT  -->
        <ScrollView Grid.Row="1">
            <!--  Контейнер ширины берет от родительской сетки  -->
            <VerticalStackLayout Padding="20">

                <!--  Карточка формы  -->
                <Frame Padding="20" Style="{StaticResource MedCard}">
                    <Grid RowSpacing="15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!--  Препарат  -->
                        <Label Grid.Row="0" Style="{StaticResource Caption}" Text="Препарат" />

                        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Border
                                Grid.Column="0"
                                HeightRequest="44"
                                Margin="0"
                                Padding="10,0"
                                Style="{StaticResource MedInputContainer}">
                                <Label
                                    LineBreakMode="TailTruncation"
                                    Text="{Binding SelectedMedicine.TradeName, TargetNullValue='Не выбран'}"
                                    TextColor="{StaticResource TextPrimary}"
                                    VerticalOptions="Center" />
                            </Border>

                            <Button
                                Command="{Binding PickMedicineCommand}"
                                Grid.Column="1"
                                HeightRequest="44"
                                Padding="18,0"
                                Style="{StaticResource PrimaryButton}"
                                Text="Выбрать" />
                        </Grid>

                        <!--  Период  -->
                        <Label Grid.Row="2" Style="{StaticResource Caption}" Text="Период" />

                        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="12">
                            <VerticalStackLayout Spacing="6">
                                <Label Style="{StaticResource Caption}" Text="Начало" />
                                <Border
                                    HeightRequest="44"
                                    Margin="0"
                                    Padding="10,0"
                                    Style="{StaticResource MedInputContainer}">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Entry
                                            x:Name="StartDateEntry"
                                            Keyboard="Numeric"
                                            MaxLength="10"
                                            Placeholder="ДД.ММ.ГГГГ"
                                            Text="{Binding StartDate}"
                                            TextChanged="OnDateEntryTextChanged" />
                                            
                                        <Label Grid.Column="1" Text="📅" VerticalOptions="Center" Margin="5,0"/>
                                        <DatePicker Grid.Column="1" Date="{Binding StartDatePickerValue}" Opacity="0.01" WidthRequest="40" BackgroundColor="Transparent"/>
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>

                            <VerticalStackLayout Grid.Column="1" Spacing="6">
                                <Label Style="{StaticResource Caption}" Text="Окончание" />
                                <Border
                                    HeightRequest="44"
                                    Margin="0"
                                    Padding="10,0"
                                    Style="{StaticResource MedInputContainer}">
                                    <Grid ColumnDefinitions="*,Auto">
                                        <Entry
                                            x:Name="EndDateEntry"
                                            Keyboard="Numeric"
                                            MaxLength="10"
                                            Placeholder="ДД.ММ.ГГГГ"
                                            Text="{Binding EndDate}"
                                            TextChanged="OnDateEntryTextChanged" />
                                            
                                        <Label Grid.Column="1" Text="📅" VerticalOptions="Center" Margin="5,0"/>
                                        <DatePicker Grid.Column="1" Date="{Binding EndDatePickerValue}" Opacity="0.01" WidthRequest="40" BackgroundColor="Transparent"/>
                                    </Grid>
                                </Border>
                            </VerticalStackLayout>
                        </Grid>

                        <!--  Дозировка  -->
                        <Label Grid.Row="4" Style="{StaticResource Caption}" Text="Дозировка" />
                        <Border
                            Grid.Row="5"
                            HeightRequest="44"
                            Padding="10,0"
                            Style="{StaticResource MedInputContainer}">
                            <Entry
                                Placeholder="Напр. 1 таб. 2 раза/день"
                                ReturnType="Done"
                                Text="{Binding Dosage}" />
                        </Border>

                        <!--  Заметки  -->
                        <Label Grid.Row="6" Style="{StaticResource Caption}" Text="Заметки (необязательно)" />
                        <Border
                            Grid.Row="7"
                            BackgroundColor="Transparent"
                            HeightRequest="140"
                            Padding="12"
                            Stroke="{StaticResource BorderLight}"
                            StrokeShape="RoundRectangle 12"
                            StrokeThickness="1">
                            <Editor
                                AutoSize="TextChanges"
                                BackgroundColor="Transparent"
                                Placeholder="Комментарий врача..."
                                Text="{Binding Notes}" />
                        </Border>

                        <!--  Ошибка  -->
                        <Label
                            Grid.Row="8"
                            FontSize="13"
                            IsVisible="{Binding HasError}"
                            Text="{Binding ErrorText}"
                            TextColor="{StaticResource Error}" />

                        <!--  Кнопки (Сохранить / Отмена)  -->
                        <Grid Grid.Row="9" ColumnDefinitions="*,*" ColumnSpacing="15">
                            <Button
                                Grid.Column="0"
                                BackgroundColor="Transparent"
                                BorderColor="{StaticResource TextSecondary}"
                                BorderWidth="1"
                                Command="{Binding GoBackCommand}"
                                CornerRadius="10"
                                FontSize="14"
                                HeightRequest="44"
                                Text="Отмена"
                                TextColor="{StaticResource TextSecondary}" />

                            <Button
                                Grid.Column="1"
                                Command="{Binding SaveCommand}"
                                CornerRadius="10"
                                FontAttributes="Bold"
                                FontSize="14"
                                HeightRequest="44"
                                Style="{StaticResource PrimaryButton}"
                                Text="Сохранить" />
                        </Grid>

                        <!--  Удалить  -->
                        <Button
                            Grid.Row="10"
                            BorderColor="{StaticResource Error}"
                            Command="{Binding DeleteCommand}"
                            IsVisible="{Binding IsEditMode}"
                            Margin="0,5,0,0"
                            Style="{StaticResource SecondaryButton}"
                            Text="Удалить"
                            TextColor="{StaticResource Error}" />

                    </Grid>
                </Frame>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
