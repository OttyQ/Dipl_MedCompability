<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    BackgroundColor="{StaticResource AppBackground}"
    Shell.NavBarIsVisible="False"
    Title="Карта пациента"
    x:Class="MedCompatibility.Pages.Doctor.DoctorPatientCardPage"
    x:Name="RootPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:controls="clr-namespace:MedCompatibility.Pages.Shared.Controls"
    xmlns:models="clr-namespace:MedCompatibility.Models"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Grid>
        <!--  фон  -->
        <controls:BackgroundBubblesView BottomBubbleBounds="0, 0, 0, 0" />

        <Grid RowDefinitions="Auto,*" Style="{StaticResource PageContentGrid}" WidthRequest="{OnPlatform WinUI=800, MacCatalyst=800}">
            <!--  Шапка  -->
            <controls:CustomHeader
                Grid.Row="0"
                Margin="20,20,20,10"
                Title="{Binding PatientFullName}" />

            <!--  Контент  -->
            <ScrollView Grid.Row="1" Padding="20,0">
                <VerticalStackLayout HorizontalOptions="Fill" Spacing="16">

                    <!--  Инфо о пациенте  -->
                    <Border
                        BackgroundColor="{StaticResource Surface}"
                        Padding="16"
                        StrokeShape="RoundRectangle 20"
                        StrokeThickness="0">
                        <Border.Shadow>
                            <Shadow
                                Brush="Black"
                                Offset="0,4"
                                Opacity="0.05"
                                Radius="8" />
                        </Border.Shadow>
                        <Grid ColumnDefinitions="60,*" ColumnSpacing="16">
                            <!--  Иконка профиля  -->
                            <Border
                                BackgroundColor="#E3F2FD"
                                HeightRequest="60"
                                StrokeShape="RoundRectangle 30"
                                StrokeThickness="0"
                                WidthRequest="60">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="30"
                                    HorizontalOptions="Center"
                                    Text="👤"
                                    TextColor="{StaticResource Primary}"
                                    VerticalOptions="Center" />
                            </Border>

                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                                <Label
                                    FontAttributes="Bold"
                                    FontSize="18"
                                    Text="{Binding PatientFullName}"
                                    TextColor="{StaticResource TextPrimary}" />
                                <Label
                                    FontSize="14"
                                    Text="{Binding PatientLogin, StringFormat='@{0}'}"
                                    TextColor="{StaticResource TextSecondary}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Border>

                    <!--  Кнопка добавить  -->
                    <Button
                        Command="{Binding AddMedicineCommand}"
                        CornerRadius="14"
                        FontAttributes="Bold"
                        FontSize="16"
                        HeightRequest="54"
                        Style="{StaticResource PrimaryButton}"
                        Text="+ Добавить препарат">
                        <Button.Shadow>
                            <Shadow
                                Brush="{StaticResource Primary}"
                                Offset="0,4"
                                Opacity="0.3"
                                Radius="8" />
                        </Button.Shadow>
                    </Button>

                    <BoxView Color="Transparent" HeightRequest="4" />

                    <!--  Заглушка "Нет данных" (для Android)  -->
                    <VerticalStackLayout
                        HorizontalOptions="Center"
                        IsVisible="{Binding IsEmpty}"
                        Padding="0,50"
                        Spacing="10"
                        VerticalOptions="Center">
                        <Label
                            FontSize="40"
                            HorizontalOptions="Center"
                            Opacity="0.5"
                            Text="💊" />
                        <Label
                            FontSize="16"
                            HorizontalOptions="Center"
                            Text="Нет назначений"
                            TextColor="{StaticResource TextSecondary}" />
                    </VerticalStackLayout>

                    <!--  Список назначений  -->
                    <CollectionView
                        HorizontalOptions="Fill"
                        ItemsSource="{Binding Prescriptions}"
                        SelectionMode="None">
                        <CollectionView.Triggers>
                            <DataTrigger
                                Binding="{Binding IsEmpty}"
                                TargetType="CollectionView"
                                Value="True">
                                <Setter Property="IsVisible" Value="False" />
                            </DataTrigger>
                        </CollectionView.Triggers>

                        <CollectionView.ItemsLayout>

                            <LinearItemsLayout ItemSpacing="12" Orientation="Vertical" />
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:prescription">
                                <Border
                                    BackgroundColor="{StaticResource Surface}"
                                    Padding="16"
                                    StrokeShape="RoundRectangle 16"
                                    StrokeThickness="0">

                                    <!--  ТАП по карточке = открыть редактирование  -->
                                    <Border.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={x:Reference RootPage}, Path=BindingContext.EditPrescriptionCommand}"
                                            CommandParameter="{Binding .}"
                                            x:DataType="{x:Null}" />
                                    </Border.GestureRecognizers>

                                    <Border.Shadow>
                                        <Shadow
                                            Brush="Black"
                                            Offset="0,4"
                                            Opacity="0.05"
                                            Radius="8" />
                                    </Border.Shadow>

                                    <Grid RowDefinitions="Auto,Auto" RowSpacing="6">
                                        <!--  Верхняя строка: Название + Дата  -->
                                        <Grid ColumnDefinitions="*,Auto">
                                            <Label
                                                FontAttributes="Bold"
                                                FontSize="16"
                                                Text="{Binding Medicine.TradeName}"
                                                TextColor="{StaticResource TextPrimary}" />

                                            <!--  Дата Badge  -->
                                            <Border
                                                BackgroundColor="#E8F5E9"
                                                Grid.Column="1"
                                                Padding="8,4"
                                                StrokeShape="RoundRectangle 6"
                                                StrokeThickness="0">
                                                <Label
                                                    FontAttributes="Bold"
                                                    FontSize="11"
                                                    TextColor="#2E7D32">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding StartDate, StringFormat='{0:dd.MM.yy}'}" />
                                                            <Span Text=" - " />
                                                            <Span Text="{Binding EndDate, StringFormat='{0:dd.MM.yy}'}" />
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                            </Border>
                                        </Grid>

                                        <!--  Нижняя строка: Врач  -->
                                        <Label
                                            FontSize="13"
                                            Grid.Row="1"
                                            TextColor="{StaticResource TextSecondary}">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span FontAttributes="Bold" Text="Врач: " />
                                                    <Span Text="{Binding Doctor.LastName}" />
                                                    <Span Text=" " />
                                                    <Span Text="{Binding Doctor.FirstName}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>

                    <!--  Кнопка удаления пациента из списка  -->
                    <Button
                        BackgroundColor="Transparent"
                        BorderColor="#E53935"
                        BorderWidth="1.5"
                        Command="{Binding DeletePatientCommand}"
                        CornerRadius="14"
                        FontAttributes="Bold"
                        FontSize="15"
                        HeightRequest="50"
                        Text="🗑 Удалить из моих пациентов"
                        TextColor="#E53935" />

                    <BoxView HeightRequest="40" />
                </VerticalStackLayout>
            </ScrollView>
        </Grid>


    </Grid>
</ContentPage>
